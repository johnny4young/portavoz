import PortavozCore

/// Moved to PortavozCore in M6 (DiarizationKit needs it for the
/// voiceprint encryption key); the alias keeps call sites compiling.
public typealias SecretStore = PortavozCore.SecretStore
