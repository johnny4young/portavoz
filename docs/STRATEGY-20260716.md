# STRATEGY — Repository analysis and growth plan (Jul 16, 2026)

A single implementable document: each section ends in concrete actions with a
priority (**P0** = before charging money, **P1** = next quarter, **P2** =
later) and an effort size (**S** < 1 day, **M** = days, **L** = weeks).
Written against v0.6.0 + the Band 0–6B1 refactor state, revised through two
research/refinement iterations (Jul 16–17). Inputs: the as-built specs,
PRODUCT.md, ROADMAP.md, GAPS.md, refactor-20260714.md, `docs/evidence/`, the
live website, and four live-web research passes — competitors, growth
playbooks, monetization stack + WWDC 2026, and wedge validation (sources
cited inline in §3/§5/§8/§9/§11).

**This is the single go-forward planning document.** Standalone
feature/improvement/plan Markdown files get absorbed here (so far:
`docs/design/claude-design-brief.md` → §17) and may then be deleted by the
maintainer; the AGENTS.md-listed sources of truth (ROADMAP, specs, DECISIONS,
ARCHITECTURE, PRODUCT, GAPS, RELEASING, IOS, the refactor band plan) remain
authoritative for their own domains — this document sequences strategy on top
of them, never duplicates them.

How to use this in a future session: start from §0's wave that is currently
open; inside a wave, pick one action line, read the pointed files, implement
it as a normal slice (tests + docs in the same commit), and check it off
here. Sections are otherwise independent. Jargon is defined in §18.

---

## 0. Execution spine — the next 90 days (Jul → Oct 2026)

The P0s across §3–§11 are one sequenced program, not a menu. Architecture
band work (Codex) continues in parallel and is deliberately absent here.

**Wave 1 — Foundations (now → mid-Aug), parallelizable:**
macOS 27 beta validation (§3) · demo video + Wi-Fi-off GIF + README
sharpening (§7/§11) · comparison table, FAQ, proof row, email capture on the
site (§7) · privacy-receipt export-as-image (§15.1) · Companion < 5 s field
measurement (§13.4). *Done when: the betas pass, the site converts visitors
into list signups, and every claim on it carries proof.*

**Wave 2 — Monetization build (Aug → early Sep):**
Polar.sh licensing + Ed25519 offline receipt + trial (§5) · upgrade policy +
pricing page (§8) · Ethics & Data Handling page + retention controls (§5
regulated kit) · voice-enrollment polish (§5). *Done when: a stranger can
buy, activate offline, and read exactly what PRO adds and how data is
handled.*

**Wave 3 — Launch (Sep, after the macOS 27 GM ships):**
v1.0 + PRO release · coordinated launch week — Show HN + r/macapps + PH +
directories in 48 h (§11) · 9to5Mac Spotlight + FPF/GIJN + lawyer vertical
page (§11) · AlternativeTo + awesome-lists (§11). *Done when: launch week
executed, first paid licenses sold, listings live.*

**Standing rule:** anything Codex's band program ships mid-window (receipts
UI, evidence links, provenance surfaces) gets folded into the launch story —
the doc's per-section actions are updated, not duplicated.

---

## 1. Performance improvements

**Where we stand (measured, spec 08 / PRODUCT.md):** cold start 0.94 s, live
caption p95 0.53 s, post-meeting summary 3.8 s, detail first-content 91.87 ms
at 5k segments, exact/lexical/semantic retrieval p95 30.99/66.89/90.22 ms at
100k segments, Spotlight incremental refresh 426 ms at a 100k-meeting
projection (down from 22 s), a 56-minute dual-channel waveform in 109 ms,
drift 4 ms/22 min, RAM 569–795 MB peak while recording → 140–160 MB idle.
Every scale cliff known at the start of Jul 2026 (semantic O(n), full
Spotlight reindex, per-sample waveforms) was measured and removed within the
month. The work left is protecting those numbers.

**Known cliffs:**

- **Embedded MLX summary spikes ~2.4 GB transiently** (released after 120 s).
  Acceptable, but the spike is the single worst memory moment in the app.
- **No CI perf regression gate**: every number above is reproducible via CLI
  benches, but nothing fails automatically when a slice regresses one.

**Actions**

- [ ] **P1/M — Perf regression gates in CI.** A `bench-gate` job that runs
  `--bench-startup`, `bench-scale`, and the detail first-content bench against
  budget thresholds (cold < 1.5 s, search p95 < 50 ms, detail < 300 ms) and
  fails the PR on regression. Needs the self-hosted runner conversation
  (GAPS T7) or a nightly scheduled job on the maintainer's Mac.
- [x] **P1/M — Semantic retrieval inside budget without sqlite-vec.** Band 4E
  streams SQLite-owned BLOBs, scores exact vectors with Accelerate, retains
  bounded top-k rowids, and materializes only winning passages. Comparable
  wall/CPU p95 at 100k is **90.22/91.26 ms**, down 72.3%/72.2% from
  325.41/328.43 ms; absolute peak footprint falls from 50.05 to 15.66 MiB.
  D83 retains schema v7 and exact Float32 vectors; sqlite-vec, a new embedding
  table, approximation, and quantization remain unselected until a future
  measured miss.
- [x] **P2/M — Incremental search indexing.** Shipped Jul 17: meeting search
  refreshes through a protected on-device index that coalesces edit bursts
  and retries failures; the 100k-meeting projection fell from 22 s to 426 ms
  with identical searchable content.
- [ ] **P1/S — Record the embedding model with each stored vector** (ported
  idea from the sibling Gancho project, examined Jul 17). Portavoz's
  `segment.embedding` BLOB (schema v2) records nothing about which
  `NLContextualEmbedding` revision produced it — when a macOS update changes
  the model, stored vectors silently become incomparable with fresh query
  vectors and semantic quality degrades with no error. Gancho stamps every
  vector with its producing model and re-embeds on mismatch. Additive
  column + lazy re-embed path. **Note on sqlite-vec**: Gancho was examined
  for a reusable sqlite-vec implementation and has none — its
  `EmbeddingIndex` independently converged on the exact same design D83
  chose (Accelerate `vDSP_dotpr` linear scan + exact top-k; its comment
  literally says a linear scan is the right choice). Two codebases arriving
  at the same answer under measurement is validation, not duplication —
  sqlite-vec stays unselected in both until a measured miss.
- [ ] **P2/S — MLX residency knob.** Expose the 120 s idle-release as an
  advanced setting (30 s / 120 s / until quit) for 8 GB Macs; measure summary
  cold-start delta per option in the bench.
- [ ] **P2/S — Model prewarm on calendar intent**: when a calendar meeting
  starts in < 10 min and the app is open, prewarm Parakeet (the AEC warm-up
  pattern already exists in `MicrophoneSource.warmUp`). Perf work that doubles
  as the "prepared before you join" signature moment.

## 2. Architecture and patterns that serve performance

The Band 0–4 refactor already picked the right patterns — the proof is
measured: scoped GRDB observations cut Meeting Detail first-content from
522 ms to 92 ms and `MeetingHealth` p95 from 348 ms to 10 ms. The guidance is
to **finish the program, not redesign it**.

**Patterns already in place to extend, not replace:**

| Pattern | Where it lives | Extend it to |
|---|---|---|
| Scoped observation per feature (no global `libraryVersion` invalidation) | Library/Insights/Detail models | Settings panes, menu-bar panel |
| Use-cases in `ApplicationKit` (Start/Stop/Refine/Import/Regenerate) | Band 1–2 slices | Export, enrollment, dictation session |
| Single-flight priority scheduler for ML (D29) | `IntelligenceScheduler` | Batch Companion refresh + chapter titling (currently serial FM calls) |
| Durable jobs + provenance (`ProcessingJob`, `GenerationRun`) | Band 1/3 | Any new long-running work — never a bare `Task` |
| Egress gateway with classified, consented events | Band 3 slices 3F–3G | Every future network feature, no exceptions |
| Privacy receipts persisted before transport (D75) | Band 3H | The exportable receipt artifact (§15 WOW #1) |
| Redact-by-construction support export (D76) + coded failures (D77) | support/diagnostics slices | Every new failure surface routes through the coded path |
| Measure-before-storage-change discipline (D79/D81/D82 + `docs/evidence/`) | scale work | Any proposed index/cache/schema change ships with its baseline first |
| Capability types (`FoundationModelsCapability`) | Sequoia unit 4 | Translation framework, SpeechAnalyzer, per-OS gates |
| Role-scoped model loading (`loadTranscriberIfNeeded` / `loadDiarizerIfNeeded`) | Sequoia unit 5 | Whisper variants; a future unified residency ledger |

**One new proposal — `ModelResidencyLedger` (P2/M):** today each engine tracks
its own idle-generation counters. A single owner that knows every resident
model, its bytes, its last use, and applies one LRU policy would (a) simplify
the per-engine logic, (b) let the UI honestly show "what is using RAM right
now" (pairs with the privacy receipt for a "resources receipt"), and (c) give
8 GB Macs a global budget instead of per-engine heuristics.

**Actions**

- [ ] **P1/S — Finish Band 3H (privacy receipt)** — architectural keystone that
  is also the sales artifact (§15).
- [ ] **P1/M — Adopt the WWDC26 `LanguageModel` protocol** (macOS 27 SDK):
  restructure the summary/Companion providers around the public protocol so
  Apple on-device, Anthropic (official Swift package), Gemini, Ollama and MLX
  sit behind one session API — this simplifies D25's engine matrix instead of
  growing it, and AFM 3's 20B-class tier arrives for free. Validate on the
  Golden Gate betas before the September GM.
- [ ] **P1/M — App Intents semantic-index entities**: expose
  meetings/decisions/action items to the new system semantic index so
  Siri/Spotlight answer over Portavoz's local store (§9). Distribution
  disguised as an API adoption; pairs with the M14a Xcode-target work.
- [ ] **P2/M — `ModelResidencyLedger`** as above, measured before/after.
- [ ] **P2/S — Batch FM micro-calls** (chapter titles, Companion refresh) under
  one scheduler entry with cancellation, instead of N serial `.background` runs.

## 3. Compatibility across versions

**Today:** macOS 14.4+ (the `CATap` floor — honest and correct), Apple Silicon
only, Foundation Models features require macOS 26. The Sequoia stabilization
interrupt (units 1–6) just made the 14.4–15.x experience first-class: feasible
summary defaults, no silent engine fallthrough, typed setup routes, and
Companion honestly absent instead of an impossible toggle.

**Recommendations:**

- **Do not chase Intel.** ANE/MLX are the product. Document it as an anti-goal
  (§10) — the appcast already declares `arm64`.
- **Do not lower the 14.4 floor.** The tap IS the app.
- **Treat "Sequoia-class Mac" as the largest real segment** for the next two
  years. Every new intelligence feature ships with its capability enum and a
  local fallback (MLX/Ollama) or an honest absence — the unit-4 pattern.
- **The biggest compatibility gap is Companion-on-Sequoia**: it requires FM
  today. An MLX-backed classifier/answerer path would light up the flagship
  feature for every Apple Silicon Mac. (Also a differentiation move — see §15.)
- **Locales:** EN/ES complete with a per-string test gate. Portuguese (pt-BR)
  is the cheapest high-value add (LATAM adjacency; catalog infra exists).

**Actions**

- [ ] **P0/M — macOS 27 "Golden Gate" beta validation (July–August 2026)**:
  CATap capture, SpeechAnalyzer, Foundation Models, Sparkle update path, and
  the Liquid-Glass recompile on the betas — the GM lands ~September.
  Golden Gate is Apple-silicon-only, which retroactively validates the
  arm64-only decision.
- [ ] **P1 — Companion everywhere, tiered** (expanded Jul 17 after a code
  audit: the macOS 26 gate sits BEFORE the pipeline, blocking stages that
  need no model at all, and `process()` throws on FM-unavailable before ever
  reaching BYOK). One `CompanionCapability` resolving to
  full / local-mlx / local-ollama / byok / pings-only / off; the recording
  toggle always exists with a tier subtitle and a one-click route to the
  exact Settings pane (D72 pattern) — never a dead toggle, never silent
  fallthrough. Quality guards (hedge filter, dedup, `GenerationRun`
  provenance) apply to every engine. Ship in three stages:
  - [ ] **Tier 0 (S) — pings + evidence cards on every Mac 14.4+**: un-gate
    the deterministic "asked you" ping (`QuestionHeuristic.mentions`, pure
    Swift) and add citation cards — a heuristic-detected question renders
    the matching recent transcript lines (lexical retrieval, no synthesis).
    Companion exists for 100% of users immediately; evidence-first is honest
    UX, not a degraded one.
  - [ ] **Tier 1+2 (M) — BYOK + Ollama, one build**: both ride the existing
    `OpenAICompatibleChatClient` (api endpoint vs `localhost:11434`); a
    single classify-and-answer prompt per candidate keeps the < 5 s budget
    and replaces the FM classifier with the existing heuristic gate. BYOK
    keeps its D8 disclosure; Ollama is fully local on Sequoia.
  - [ ] **Tier 3 (L) — embedded MLX**: Qwen3.5-4B classifies and answers via
    JSON-mode + validation/retry (no `@Generable` outside FM). Residency: a
    "Companion session" keeps the model resident while recording on ≥ 16 GB
    Macs; 8 GB Macs default to Tier 0/1+2 or on-demand load with a
    first-card latency notice. Gate: < 5 s p95 on an M1, measured.
- [ ] **P1/S — Compatibility matrix in README/site**: one table — feature ×
  macOS 14.4/15/26/27 — generated from the capability types so it cannot rot.
- [ ] **P2/M — pt-BR localization** after the next catalog freeze.

## 4. Refactors and design changes worth making

- **Finish the Band plan** (3H receipt → Band 4 scale/outbox → later bands).
  It is working; every completed band removed a measured pressure point.
- **M14a Xcode app target** unlocks AppIntents/Siri, Quick Look, and the
  future sandbox migration (D78 proved the current shared-storage layout
  blocks the sandbox toggle) — schedule it before any App Store ambition.
- **UI code health:** the type-body cap (400 lines) keeps forcing healthy
  extractions; `MeetingDetailView` remains the largest surface — when it next
  fights the cap, split the rail (health/chapters/Companion/receipt) into its
  own feature model + view file, mirroring the Library/Insights model split.
- **Recording view banners** (mic-low, system-silent, app-tap note,
  translation download) stack vertically and compete for attention →
  consolidate into one status strip with severity ordering (design change,
  S–M).

## 5. Features needed to sell (necessary vs recommended)

**Necessary before charging (the PRO plan in PRODUCT.md is defined but has no
plumbing):**

- [ ] **P0/L — Entitlement + license system** (stack decision revised: the
  founder ALREADY uses **Lemon Squeezy**): **stay on Lemon Squeezy for
  launch** — it is a merchant of record (EU VAT handled), has built-in
  license keys with activation limits, and an integration in hand beats a
  theoretically nicer provider. Manage the known risk instead of switching
  preemptively: Lemon Squeezy is in post-Stripe-acquisition migration toward
  Stripe Managed Payments, so (a) keep the app's license seam
  **provider-agnostic** — one `LicenseVendor` protocol over
  issue/validate/deactivate so a later move to Polar.sh (the researched
  fallback: MoR, built-in keys, 5%+$0.50, open-source culture) or Stripe
  Managed Payments is configuration, not surgery; (b) define the exit
  trigger now: a forced-migration announcement, payout degradation, or
  license-API deprecation flips the switch. Status re-checked Jul 17: Stripe
  Managed Payments went **GA on Apr 29, 2026** (the sanctioned successor);
  Lemon Squeezy still functions (checkout, licensing, twice-monthly payouts)
  but has shipped nothing notable in 2026 and carries merchant complaints
  about frozen payouts and slow support — **treat the migration as a planned
  H2 2026 task**, watch for the self-serve migration tool, and build no new
  Lemon-Squeezy-specific surface. (Context: Paddle **deprecated** license-key
  fulfillment; Gumroad charges 10%; Stripe-direct is not a MoR.)
  App side regardless of vendor: validate once at activation, store a
  **CryptoKit Ed25519-signed offline receipt** in the Keychain, re-validate
  opportunistically with a long grace window — the "works offline forever"
  promise survives. 3-Mac activation limit; 14-day trial with no account
  (CleanCocoa/TrialLicensing or ~200 lines of custom Ed25519); an
  `Entitlements` capability type gating the PRO rows in PRODUCT.md; a
  "restore license" path. Piracy posture: basic key checks only — for an
  open-source app the license is a convenience-and-support purchase, and DRM
  engineering is wasted weeks.
- [ ] **P0/M — Trial UX**: full PRO for 14 days, then graceful degrade to FREE
  (never hold recordings hostage — the anti-Granola stance is the brand).
- [ ] **P0/S — Pricing page on the site** (see §7) + in-app "Upgrade" surface
  that lists exactly what PRO adds, honestly.
- [ ] **P0/M — Voice enrollment → auto-names polish** (PRO headline feature;
  code exists, needs field verification + first-run affordance).
- [ ] **P1/L — CloudKit sync (M15/CKSyncEngine)** — the second PRO pillar;
  encrypted record values, zero servers of ours. Band 6A/D92 provides the
  content-free schema-v14 mutation journal and lost-update fence; Band 6B1/D93
  adds exact-generation text-first projection, deterministic encoding, and
  atomic replay without network behavior. Encrypted CKRecord/asset transport,
  CKSyncEngine/account/retry state, consent/status UI, and iOS remain
  unimplemented Band 6B2–6D work.

**Recommended for appeal (each maps to a competitor-validated demand):**

- [ ] **P1/M — Recap draft** (email/Slack text from the summary; Gemini/
  Circleback validate) — pure local text generation, no send integration
  needed at first (copy-to-clipboard in mrkdwn already exists).
- [ ] **P1/M — Auto-record rules with guardrails** (MeetGeek pattern):
  calendar-matched "always record standup", with a loud pre-meeting banner
  and a one-tap skip. Ships only with the consent disclosure surface.
- [ ] **P1/S — Share a read-only meeting page** (M17-lite): export a single
  static HTML file of one meeting (self-contained, no server) — the
  "send the summary to someone without the app" ask, kept local-first.
- [ ] **P2/M — Watch "you were mentioned"** + **iPad PiP captions** — the two
  demo-viral moments in PRODUCT.md's signature list.
- [ ] **P1/M — Regulated-professional kit** (upgraded from P2 after the Jul 17
  validation pass — this is a NOW wedge, §11): (a) an "Ethics & Data
  Handling" page mapped clause-by-clause to the NYC Bar 2025-6 / ABA 512
  due-diligence checklist (where stored: this Mac; retention: user-owned;
  training: never; vendor access: none — open source as the verification);
  (b) the **HIPAA position**: a truly local app with no telemetry touching
  audio is **not a business associate at all** (HHS FAQ) — no BAA needed,
  stronger than offering one; (c) the **retention controls the opinions ask
  about**: per-meeting auto-delete policy, a summary-only mode (record →
  summarize → discard verbatim transcript), one-tap purge. The honest
  caveat to design for: local-only removes the third-party risks (vendor
  subpoena, training, breach, privilege waiver) but the record itself stays
  discoverable — controls over what exists are part of the product promise.

## 6. UX simplification

The app has grown many concepts (Recipes, Structures, engines, Refine,
Companion, enrollment, HUD, dictation). The DS 6a redesign already made the
main flows clean; the remaining simplification is **progressive disclosure and
vocabulary**, not layout.

**Actions**

- [ ] **P1/S — Plain-language renames in UI** (catalog-only change):
  "Refine" → "Improve transcript"; "Recipes/Structures" → one word
  ("Formats"); keep technical terms in tooltips. Both languages.
- [ ] **P1/S — One status strip in recording** (see §4) instead of stacked
  banners.
- [ ] **P1/M — "First meeting" checklist card** on the empty library (record
  10 s → see who-said-what → generate summary → done), replacing the current
  static empty state after onboarding. Time-to-value under 60 s.
- [ ] **P2/S — Settings search field** (categories grew to 6+ panes).
- [ ] **P2/S — Default path audit**: a new user should never NEED to visit
  Settings before their first good meeting (the Sequoia unit-4 feasible
  defaults already fixed the worst case; verify mic/output pickers and
  language pinning have equally safe defaults).

## 7. Website improvements to sell better

The landing is strong on identity (bilingual, benchmarks, constellation hero,
brew CTA). It is missing every *conversion* mechanism:

- [ ] **P0/S — 45–60 s demo video** above the fold (screen recording: record →
  live captions with amber "you" → stop → summary + who-said-what → ⌘K
  question). Video is the #1 missing proof; competitors lead with it.
- [ ] **P0/S — Comparison table section**: Portavoz vs Granola/Otter/Fireflies/
  MacWhisper on: local vs cloud, bot-free, speaker ID, one-time price, open
  source, works offline. Honest cells (include what they do better: mobile
  apps, integrations) — honesty is on-brand and disarms objections.
- [ ] **P0/S — Social proof strip**: GitHub stars badge (live), Homebrew
  install count if available, "as seen on" once press exists.
- [ ] **P0/S — FAQ section** (screen-recording permission, which Macs, where
  data lives, how updates work, "is it really free?").
- [ ] **P1/S — Email capture** ("get the PRO launch discount") — the only
  pre-launch asset that compounds; a static form → Buttondown/Listmonk.
- [ ] **P1/M — SEO content**: one honest "Portavoz vs X" page per major
  competitor + one use-case page per query cluster ("record Zoom meeting
  locally on Mac", "meeting transcription without bots", Spanish
  equivalents) + **the bar-opinion queries nobody owns yet** (searches
  around NYC Bar 2025-6 / ABA 512 compliant notetaking — §11's regulated
  wedge notes the mover here will be hard to displace). The site is static
  Cloudflare Pages — plain HTML pages are enough.
- [ ] **P1/S — Privacy-respecting analytics** (Cloudflare Web Analytics — no
  cookies) to measure CTA conversion; publishing the numbers is on-brand.
- [ ] **P2/S — Press kit page** (icon, screenshots, one-paragraph pitch,
  founder contact).

## 8. Pricing model — assessment and recommendation

**Current target (PRODUCT.md): one-time ~$69, launch $49, generous FREE.**

Assessment against the July 2026 market (see §9): cloud subscriptions cluster
at **$8–20/u/mo annual, median ≈ $15** ($16–30 monthly); lifetime/one-time
survives — and thrives — only among indie native Mac apps (MacWhisper €59
lifetime is the proven benchmark; superwhisper $249 lifetime; Whisper Notes
$6.99); Granola now monetizes a 25-note free-history cap and Fathom clawed
back its free tier, feeding exactly the subscription-fatigue the one-time
model converts. Notably, **"free MIT core + one-time paid Pro" is untried in
the entire niche** — Meetily and Anarlog both chose open-core with $10–15/u/mo
subscriptions. Being the first pay-once open-source meeting assistant is
itself a marketing hook. **Verdict: the model is right. The number ($69, $49
launch) is right — comfortably above MacWhisper (more product) and far under
superwhisper. The gaps are policy details:**

- [ ] **P0/S — Write the upgrade policy before launch**: v1.x updates free
  with the license; a future v2 is a paid upgrade at ~50% for owners
  (Things/Sketch pattern). Announcing it up front converts subscription-fatigue
  buyers who fear abandonment.
- [ ] **P0/S — Keep FREE genuinely complete** (unlimited local record/
  transcribe/diarize/summarize, as PRODUCT.md already commits). The FREE tier
  is the top of the funnel and the moral high ground; never move core capture
  behind pay.
- [ ] **P1/S — Team pack** (5 licenses, ~$199) — zero infra, pure SKU.
- [ ] **P1/S — EDU/OSS-maintainer discount** (30–40%) — cheap goodwill in the
  exact communities that amplify OSS tools.
- [ ] **Do not join Setapp** (subscription framing contradicts the pitch,
  ~30% cut, and the buyer overlap with Homebrew users is small).
- [ ] **P2/S — Optional paid "supporter" tier on GitHub Sponsors** for the
  FREE users who want to pay without needing PRO.
- [ ] **P2/L — Mac App Store as a later dual-SKU channel, eyes open**: CATap
  is effectively not sandbox-viable, but a **ScreenCaptureKit audio-capture
  backend works sandboxed** (precedent: Meeting-Rec ships that way, and
  MacWhisper runs a Gumroad-one-time + MAS-subscription twin). Requires a
  second capture backend and Screen Recording permission UX — do not block
  the PRO launch on it; revisit for discovery once direct sales are proven.
- [ ] **P1/S — LATAM purchasing-power pricing from day one**: Polar/Paddle
  support regional pricing; list ~$69 US/EU with ~$29–39 regional
  equivalents. One-time + PPP travels far better in LATAM than USD
  subscriptions (validated in the Jul 17 wedge pass).

## 9. Competitive landscape (web-checked Jul 16, 2026) and how to win

> Live-web research pass (all sources accessed Jul 16, 2026; GitHub stars via
> the API the same day). PRODUCT.md keeps the deeper per-competitor teardown;
> this section carries what changed and what it means.

**The market in one paragraph.** The category consolidated upward: Granola is
at a $1.5B valuation (Series C $125M, Mar 2026), Fireflies $1B and profitable,
Otter past $100M ARR and pivoting to enterprise "knowledge engine", Plaud at
$250M ARR on hardware. Market ≈ $3.9–4.3B in 2026 growing ~25%/yr; enterprise
is ~70% of spend but **prosumer is the fastest-growing segment — and the
well-funded players are all fleeing it upmarket**. Bot-free capture became
table stakes (Granola, Krisp, Jamie, Circleback, Bluedot, Notion followed);
**fully-local processing did not** — that is the active frontier and exactly
Portavoz's wedge (Granola's own security page confirms Deepgram/AssemblyAI +
OpenAI/Anthropic in its path).

**Three tailwinds that appeared in 2025–26 (use them in copy):**

1. **The bots are being evicted — now on a hard schedule** (re-verified
   Jul 17): Teams Phase 1 (lobby bot-check, organizers must manually admit
   detected AI bots) completed worldwide rollout by mid-June 2026; **Phase 2
   — admin auto-block of all detected external bots — begins August 2026**,
   hitting Otter/Fireflies/Fathom/Read.ai tenants. Bot-based competitors
   lose their capture path; device-side capture is structurally immune. The
   bot-fatigue narrative reached mainstream press (AP News, Jul 12).
2. **Cloud notetakers are in legal trouble**: the consolidated Otter class
   action's motion-to-dismiss hearing was held **July 15, 2026** — a ruling
   could land any day (re-check within a week; either outcome is a news
   moment to ride); the NYC Bar's Formal Opinion 2025-6 (Dec 2025) warned
   lawyers off cloud notetakers; universities are blocking bots. Every such
   event manufactures demand for "audio never leaves the machine".
3. **Free tiers are shrinking** (Granola capped history at 25 notes; Fathom
   capped advanced summaries at 5/mo) — subscription fatigue plus clawbacks
   make "pay once, own it forever" land harder than ever.

**Condensed field (Jul 2026):**

| Product | Local processing? | Bot-free? | Price | One-line read |
|---|---|---|---|---|
| Granola | ❌ (cloud ASR+LLM) | ✅ | Free capped / $14–35/u/mo | Category UX leader; privacy story is capture-only |
| Otter | ❌ | ❌ bot | $8–30/u/mo | Enterprise scale; worst consent reputation, class action |
| Fireflies | ❌ | ❌ bot | $10–39/u/mo | Cheap + profitable; AI-credits upsell trap |
| Fathom | ❌ | ❌ bot | Free capped / $15–34 | Loved free tier, now clawed back |
| Jamie | ❌ (EU cloud) | ✅ | €25–99/mo | Sells GDPR sovereignty, not locality |
| Apple Notes (macOS 26) | ✅ | ✅ | Free | No third-party-app capture, no diarization, no library |
| MacWhisper | ✅ | ✅ | **€59 one-time** | Beloved indie; transcription-first, not a meeting brain |
| Meetily (MIT, 25.1k ⭐) | ✅ | ✅ | Free / Pro $10/u/mo | OSS reference; Tauri, rough edges, zero RAG/chat |
| Anarlog (MIT, 8.8k ⭐) | ✅ free tier | ✅ | Free / $15/mo cloud Pro | Closest rival; 3 renames in a year, pushes cloud Pro |
| Basil AI | ✅ (Apple frameworks) | ✅ | ~$100/yr | App-Store on-device wrapper; thin moat |
| Cluely | ❌ | overlay | $20–75/mo | "Cheating" stigma, 83k-user breach, inflated ARR |
| Screenpipe (20.2k ⭐) | ✅ | ✅ | OSS + agents | Lifelogger, not a meeting tool |

**Gaps nobody fills (verified Jul 2026) — Portavoz maps onto nearly all:**

1. 100% local + open source + **polished native macOS** app (Meetily = rough
   Tauri; Anarlog = brand-churned, cloud-leaning).
2. **Open-source live in-meeting companion** — Amurex is dormant (last push
   May 2025); Cluely is radioactive; Otter's agent is cloud enterprise. The
   slot is empty; Portavoz's Companion is the only live entrant.
3. **Bilingual EN/ES as product** (not "100 languages" ASR marketing) — the
   LATAM/Spain professional market is structurally unserved.
4. **One-time purchase for a full meeting assistant** (MacWhisper proves the
   price point for transcription alone).
5. **On-device cross-meeting RAG/Ask** — the enterprise clouds do it
   server-side; nobody does it locally. Portavoz already ships it.
6. **Post-Rewind orphans**: Meta bought Limitless (Dec 2025) and shut Rewind
   down — privacy-minded local-memory users lost their product and nobody has
   courted them.
7. **Regulated-prosumer compliance story** (lawyers/doctors/finance) — the
   NYC Bar opinion created demand only EU-cloud Jamie half-answers.

**WWDC June 2026 verdict (researched Jul 17) — no Sherlock, and a gift:**
macOS 27 "Golden Gate" (ships ~Sept 2026, Apple-silicon-only) is an
explicitly Snow-Leopard-style refinement year: **no system-audio meeting
capture, no OS meeting notes, no SpeechAnalyzer update**. The practical
Sherlock window now runs to **WWDC 2027 — ship PRO well inside it**. The
gift: the new Siri AI is **powered by Google Gemini in the cloud**, which
sharpens the positioning to a knife edge ("local by design — even Siri now
sends your words to Google's cloud"). What Apple DID ship helps us: AFM 3
(a 20B sparse-MoE tier on Macs — re-benchmark summaries on the macOS 27
SDK), multimodal FM input, a public `LanguageModel` protocol (Apple /
Anthropic-official-package / Gemini behind one session API), free Private
Cloud Compute under 2M downloads, and an **App Intents semantic index** —
expose meetings/decisions/action items as entities and Siri/Spotlight answer
"what did we decide about X?" **from Portavoz's local store**: the
platform's headline feature becomes distribution instead of competition.
Pre-GM checklist: validate CATap + SpeechAnalyzer + FM paths on the Golden
Gate betas (July–August).

**Threats to respect (refreshed Jul 17):** Apple absorbing the low end
off-cycle (a `.x` Notes update could still learn to hear Zoom — reassess at
each point release and at WWDC 2027; macOS 27 beta 3 shows **no capture-stack
changes** per Rogue Amoeba's notes — re-check at beta 4/5); the viral
SpeechAnalyzer benchmark (HN front page Jul 13, 2.12% WER vs Whisper Small's
3.74%) publicly validates Apple's on-device stack **but lowers the floor for
every Mac indie wrapping the same API** — the moat stays diarization, speaker
memory, evidence links, bilingual, and workflow, never raw transcription;
Granola's war chest, Android launch (Jul 1) and defensive "local-first vs
cloud" content; Basil AI marketing on-device privilege protection straight at
lawyers (still negligible traction: 10 App Store ratings — the position is
being claimed but not yet won); a **thickening tail of tiny bot-free local
Mac apps** (Nod, Biscotti, Speechmark, LokalBot — all under 20 HN points, no
distribution) confirming both demand and urgency; SEO remains a content war
among micro-competitors. **Softened threats:** Meetily's star gravity now
comes with a 6-week code stall (25,235 stars, last push Jun 5) and Screenpipe
**left the meeting lane entirely** (repo moved, repositioned to agent/workflow
recording) — the polished-native-OSS slot is more open than it was on Jul 16.

**How to win (positioning, unchanged and validated):** nobody combines
local-first + speaker identity + developer workflow + bilingual + one-time +
MIT. Every competitor holds ~2 of the 6. The strategy is not to out-feature
Otter; it is to be the **obvious** choice for the intersection: privacy-
conscious Mac professionals, developers, and bilingual teams — then expand
outward from a defensible core.

- The "local open source" throne is **vacant** (Anarlog pivoted away; Meetily
  is cross-platform Tauri with zero RAG/chat; MacParakeet is dictation-first
  GPL). Portavoz can own "the serious native one".
- The Companion window is real and closing: Teams Facilitator lands
  ~Aug–Sep 2026. Shipping the field-verified, honest, local Companion story
  BEFORE that (with latency receipts) frames Microsoft as validating us.
- Apple's floor (Notes transcription, SpeechAnalyzer) rises yearly: the rule
  in PRODUCT.md holds — never build a core feature Apple will obviously
  absorb; keep living above the floor (identity, history, workflow, bilingual).

## 10. What NOT to become (anti-goals)

1. **No bots that join calls** — the category's most-hated pattern.
2. **No accounts, no cloud-default, no telemetry-default** — ever.
3. **No subscription for local features.** If recurring revenue is ever
   needed, it can only price genuinely recurring costs (a hosted relay), never
   local capability.
4. **No archive hostage** (the Granola lever). FREE users' data is never
   time-locked, degraded, or export-restricted.
5. **No rug-pull relicensing.** MIT stays MIT (the Anarlog pivot vacated the
   throne we now claim; repeating it would destroy the moat).
6. **No Electron/Tauri rewrite, no Windows port** while the Mac edge is the
   product. Cross-platform dilution is how the natives die.
7. **No Cluely-style covert overlay.** The Companion is transparent,
   disclosed, and never speaks for the user. "Cheating tool" stigma is a
   brand-killer in the enterprise-adjacent market.
8. **No enterprise checkbox sprawl** (SSO/DLP/compliance dashboards) before
   the prosumer core is won.
9. **No feature Apple ships "basic" within ~2 years** as a headline.
10. **No dark-pattern growth** (nag screens, forced sharing, SEO spam).

## 11. Marketing strategies (concrete, evidence-backed)

> Live-web research pass, Jul 16, 2026. Every strategy below has a working
> precedent in this exact category. The one lesson repeated across every case
> study (MacWhisper 250k downloads/~$1.9M lifetime on Gumroad; Meetily 0→25k
> stars in 16 months; Granola's beachhead launch): **one crisp repeatable
> sentence with proof next to it**. Ours: *"Open-source (MIT), 100% local
> meeting notes for your Mac — in English and Spanish. No bot, no account, no
> cloud."* Every channel is a delivery vehicle for that sentence.

**Channel evidence worth internalizing:**

- **Meetily's 25k stars did NOT come from launch pops** — its Show HN got 4
  points. They came from grind: a keyword-loaded repo description
  ("Otter alternative", "Granola alternative" — GitHub search SEO), dev.to
  comparison posts, and hitting GitHub Trending twice via star-velocity
  bursts (~50–300 stars/day makes the list; synchronized pushes beat
  trickles).
- **r/macapps is the best effort/impact channel for a Mac indie**: analysis
  of 500 top posts shows comparison/alternative posts average 346 upvotes
  (best-performing type); free/OSS apps average 260; the winning frame is a
  personal narrative + honest comparison + demo GIF; AI-slop smell kills.
- **9to5Mac's Indie App Spotlight is an open submission pipeline** (weekly
  series, direct email) — designed for exactly this app.
- **The privacy message that converts is incentive alignment + verifiable
  proof**, per Obsidian/Proton/Signal/Ente: "I literally cannot see your
  meetings" (one-time purchase = no data business), open source as trust
  proof, and demo artifacts — a Wi-Fi-off recording demo, a network-monitor
  screenshot showing zero outbound connections mid-transcription,
  sha256-pinned models said out loud.
- **Granola's growth mechanic is copyable one level deeper**: it won by
  contrast with visible bots; Portavoz contrasts with bots AND clouds. Its
  beachhead lesson: target high-signal-density users under confidentiality
  pressure (founders, lawyers, HR, journalists), not "everyone with meetings".
- **LATAM is the underserved wedge**: 72% of LATAM companies adopted
  remote/hybrid work (vs 41% North America); Spanish-language roundups
  explicitly flag that English-first tools underperform in Spanish; every
  player courting that market is cloud. Nobody owns local-first meeting AI
  for Spanish speakers. A one-time USD purchase also travels better in LATAM
  than a monthly SaaS subscription. Channels: the midudev community/Discord
  (~46k), the mouredev newsletter (100k+), Genbeta/Xataka, Spanish
  productivity YouTube; Spanish comparison queries (e.g. "alternativa a
  Otter", "transcribir reuniones sin nube") have little content competition.

**Action plan (ordered by effort/impact):**

- [ ] **P0/S — README sharpening**: the README already leads with screenshots,
  badges, and the pitch — what is missing per the research: (a) a 15 s
  **animated GIF/video** at the very top (recording transcribed with the
  Wi-Fi menu visibly OFF — motion converts where stills do not), (b)
  **alternative-to keywords in the repo description and topics**
  ("otter-alternative", "granola-alternative", "meeting-notes",
  "local-first", "whisper", "privacy") for GitHub search SEO — Meetily's
  proven lever, (c) a star-history chart. Multiplies every other channel.
- [ ] **P0/S — Privacy-proof landing additions** (§7's video + a proof row:
  MIT badge, "no account", "works offline", network-monitor screenshot).
- [ ] **P0/S — 9to5Mac Indie App Spotlight submission** + MacStories pitch
  (short personal email, press kit, the privacy hook).
- [ ] **P0/M — Coordinated launch week** (Tue–Thu): Show HN (literal title,
  maker comment with the Swift/CATap/on-device stack and one honest
  limitation, answer everything in hour one) + r/macapps narrative post +
  Product Hunt + Uneed/Peerlist, all within 48 h to trigger a GitHub
  Trending burst.
- [ ] **P1/S — AlternativeTo listing** (mark as alternative to Otter,
  Fireflies, Granola, MacWhisper, Meetily) + awesome-list PRs (awesome-mac,
  open-source-mac-os-apps, awesome-native-macosx-apps). Hours of work,
  compounds for years.
- [ ] **P1/S — Build-in-public cadence** (X + Bluesky + Mastodon, same clip):
  one measured-number artifact per release — the benchmarks ARE the content.
- [ ] **P0/M — Regulated-professionals wedge (validated Jul 17: a NOW
  wedge)**: (a) the §5 "Ethics & Data Handling" page mapped to NYC Bar
  2025-6 / ABA 512 / the HHS not-a-business-associate test; (b) pitch
  **Freedom of the Press Foundation and GIJN** for inclusion in their
  transcription-security guides (they already steer journalists to local
  Whisper tools — being listed IS the proof for that audience); (c) one
  vertical landing page for **solo/small-firm lawyers** ("client meetings,
  depo prep, intake calls — no third party in the privilege analysis").
  Urgency: Basil AI is already marketing this exact position; the regulatory
  wave (Dec 2025–Mar 2026) is 8 months old and the mover who owns the
  bar-opinion SEO will be hard to displace. Price umbrella is enormous
  (Dragon ~$780/yr, DAX $2.4–7k/yr, therapist scribes $240–840/yr vs $69
  once).
- [ ] **P1/M — Bilingual wedge, resequenced (validated Jul 17)**: the
  Spanish-speaking market is a LATER geography but a NOW differentiator —
  the most valuable Spanish-speaking buyers are **bilingual US
  professionals** whose code-switched meetings mainstream tools butcher
  (Otter renders Spanish as broken English; no Spanish-first local player
  exists). Lead asset: a side-by-side video of a code-switching EN/ES client
  meeting — their gibberish vs Portavoz. Then Spain (3.3M remote workers,
  ~12% Mac share) before LATAM; Spanish-first landing + Applesfera/Genbeta
  pitches + the midudev/mouredev dev communities; PPP pricing already
  covered in §8.
- [ ] **P1/M — YouTube reviewer circuit** (10 personalized emails, 30 s
  private demo, free PRO license, no embargo demands).
- [ ] **P2/M — Homebrew install count as social proof** (public analytics
  API) once numbers are respectable.
- [ ] **Setapp: NOT a launch channel** — subscription economics conflict with
  one-time positioning; revisit only after PRO ships, as discovery.
- [ ] **Short-form video: experiment only** (one 60 s vertical "Wi-Fi off"
  demo; invest further only on traction).

## 12. Language / stack changes for differentiation?

**No change. The stack IS the differentiation.** Swift 6 strict + SPM +
CoreML/ANE + MLX is what produces 0.53 s live captions at 600 MB RAM while the
Tauri/Electron competitors ship heavier and slower; PRODUCT.md's measured
table is the proof. Recommendations at the edges only:

- Adopt the **Xcode app target** (M14a) for AppIntents/extensions — SPM
  remains the source of truth for Kits.
- Keep **CLI + MCP** as first-class Swift targets (developer moat).
- Consider **Rust only if** a future cross-platform *core* (not UI) must be
  shared with iOS+server — currently nothing qualifies.
- The honest differentiator to *say out loud*: "native Swift, no web view,
  no sidecar processes — and here are the numbers".

## 13. Roadmap analysis

**Strengths:** measurable acceptance criteria per milestone; honest
anti-promises (iOS capture); phases match reality; the Band program runs in
parallel without freezing product work (the Sequoia interrupt proved the
protocol absorbs field feedback).

**Gaps found:**

1. **Monetization has no milestone.** PRO is specified (PRODUCT.md) but no
   M-number covers entitlements/payments/pricing page. Insert **M-PRO**
   (see §5 P0 items) BEFORE Phase 3 iOS — iOS is a cost center until the Mac
   product earns.
2. **Marketing has no milestone after M9.** Growth is a listed "next focus"
   but nothing is scheduled. Attach §11's P0 items to the v1.0 release train.
3. ~~Band 3H should gate the PRO launch~~ — **resolved while this document
   was being written**: privacy receipts shipped (D75, Jul 16). The launch
   dependency inverts: make the receipt a pricing-page hero (§15.1).
4. **Companion field verification is deadline-driven** (Teams Facilitator
   ~Aug–Sep). Schedule the < 5 s field measurement now, not opportunistically.
5. **M14 (iOS) is a whole-quarter phase**; consider slicing "M14-min": the
   in-person recorder alone (no sync, no Live Activity) to establish App
   Store presence earlier and start the CKSync learning curve.
6. **The September macOS 27 GM is an unowned deadline**: beta validation
   (§3 P0) and the `LanguageModel`/semantic-index adoptions (§2) belong to a
   named pre-GM checklist, not to opportunistic slices — Golden Gate is a
   refinement year (§9), which makes this cheap, but only if scheduled.

## 14. Is the architecture maintainable and fitting? How to improve it

**Yes — unusually so for a solo project**: strict Swift 6, 650+ tests
including characterization suites, an 80+ entry decision log, as-built specs
separated from plans, a refactor program with a feature-parity contract, and
provenance/egress architecture most teams never build. The risks are
elsewhere:

- **Bus factor = 1.** Mitigations: CONTRIBUTING.md + labeled good-first
  issues; keep AGENTS.md the single operating guide (it already is); the
  decision log is the onboarding asset.
- **CI cannot run model-gated tests** (GAPS T7) — a monthly manual full-gate
  run on the maintainer's Mac, written into RELEASING.md, is the cheap fix.
- **UI-test harness fragility** (testmanagerd wear causes false mass
  failures): add `killall testmanagerd` to `test-ui-preflight` and a
  one-retry policy for the known "row not hittable" signature. Document the
  tree-stability fingerprint check used during concurrent-agent QA.
- **Docs sprawl**: refactor-20260714.md is load-bearing but huge; once Band 4
  lands, fold the completed bands into ARCHITECTURE.md and keep only live
  bands in the plan doc.

## 15. WOW-factor ideas (novel in the market)

Ranked by (uniqueness × feasibility on the existing codebase):

1. **The privacy receipt as a shareable artifact** — the hard part already
   shipped (D75: content-free receipts persisted before any transport; the
   detail rail renders them). What remains is pure leverage: an
   export-as-image card — "This meeting: 100% on-device. 0 bytes left this
   Mac." — plus the marketing surface (§7/§11). Nobody can copy it without
   being local-first. It turns the architecture into marketing. Effort now: S.
2. **Companion cites your own history live** (RAG + Companion join): when the
   room asks "what did we decide about X?", the card answers WITH a citation
   that jumps to the past meeting's audio. No competitor has cross-meeting
   live answers, cloud or local. The building block shipped Jul 17:
   summaries/decisions already carry clickable transcript evidence with
   stale-evidence handling — the live card reuses that exact machinery
   against the cross-meeting index.
3. **iPhone as room mic (Continuity) → 3-channel hybrid meetings** (M14
   adjacency): the room, the call, and you — separately attributed. Unique to
   native Apple stack; demo-viral.
4. **Floating translated captions over any call** (iPad PiP first, Mac
   window later): subtitles in your language over Zoom — composable with the
   translation engine. Krisp sells interpretation as a service; this is
   on-device.
5. **Watch tap when you're mentioned** — 3 s demo, instantly understood.
6. **"Ask your week" as an OS-level surface** (⌘K exists): global hotkey +
   menu-bar answer with clickable citations — Jamie's ⌘J validated demand;
   ours answers from a local index.
7. **Live meeting-cost/health overlay** (data exists in MeetingHealth):
   optional, private, "measured not judged" — talk-balance nudges only you
   see.

## 16. Becoming world-class (the quality bar)

World-class here = **provable numbers + provable privacy + release
discipline**, sustained:

- **Publish the benchmark dashboard**: one page (site) auto-fed by the CLI
  benches per release — latency, DER, RAM, search — with the Mac model listed.
  The seed already exists: `docs/evidence/*.json` (the D82 semantic baseline
  is committed evidence) — render those files on the site instead of
  inventing a new pipeline. "Measured, not promised" becomes checkable
  history, not a slogan.
- **Quality gates already exist — wire them to releases**: swift test +
  strict lint + UI suite + distribution verifier are all in place; RELEASING
  should require the bench suite and the field-verification checklist per
  minor release (the Jul 16 interrupt showed field feedback lands fast when
  scheduled).
- **Accessibility as a differentiator**: a VoiceOver-audited transcript view
  (live regions for captions) would be unique in the category and is the
  right thing for a tool whose output is speech; add an a11y audit milestone.
- **Security posture**: keep the honest non-sandbox statement (D78), publish
  SECURITY.md response SLOs, and add a reproducible-build story when the
  Xcode target lands; the egress-gateway + receipts architecture is already
  ahead of the market — say so publicly with the receipt artifact.
- **Support loop — already shipped, now use it**: D76's redact-by-construction
  local support export and D77's coded failures exist; the remaining work is
  the loop itself — an issue template that asks for the support file, and
  triage habits around the stable failure codes.
- **The bar to hold**: every release keeps — tests green, benches within
  budget, zero Spanish-prose regressions in EN sources, localization complete,
  CHANGELOG user-facing, notarized + stapled + cask verified. That discipline,
  visible in public, IS the world-class signal for this category.

## 17. Design system reference

> Absorbed from `docs/design/claude-design-brief.md` (Jul 14) so this
> document is the single go-forward plan; the original brief may be deleted.
> The Claude Design project it fed already produced the shipped 6a design
> system — what remains durable here is the token record and the open items.

**De facto tokens (as-built):** app tint = system indigo; suggestion chips =
accent at 0.14 opacity in a capsule (secondary 0.08); cards `quaternary` 0.5
with radii 10/12; floating panels `.regularMaterial` radius 14; semantics
green=OK, orange=warning, red=recording/destructive, ✦ sparkles=AI
suggestion. Numbers ALWAYS `monospacedDigit`. Web identity: slate `#0e1120`
→ violet `#5226bf` gradients, single amber accent `#fdbf47` ("the amber one
is you"), Fraunces + Instrument Sans + IBM Plex Mono. SF Symbols exclusively.
Motion respects `prefers-reduced-motion`.

**The product's most repeated pattern** (design invariant worth protecting):
the suggestion chip — pure suggestion, click applies, never acts alone. Every
new AI feature adopts it rather than inventing a new interaction.

**Open design items carried forward:**

- [ ] **P2/S — List-selection accent debt**: macOS list selection uses the
  user's SYSTEM accent and can clash with app indigo — the DS must take a
  position (fixed indigo vs following system).
- [ ] **P2/S — Web-to-app identity dose**: decide how much Fraunces/amber
  enters the native app without fighting macOS conventions (currently: none —
  revisit at the v1.0 marketing push so app screenshots and site rhyme).

## 18. Glossary (product-strategy terms used above)

- **Wedge**: the entry segment — a narrow group of users where the product's
  advantage is sharpest, used to win first and expand from ("clavar la cuña").
  A NOW wedge is worth pursuing immediately; a LATER wedge waits.
- **Beachhead**: same idea as wedge, emphasizing the first defensible
  position from which to grow (Granola's was VCs).
- **Sherlocking**: Apple absorbing a third-party app's core feature into the
  OS, destroying its market (named after the app Sherlock).
- **MoR (merchant of record)**: the payment provider legally responsible for
  the sale — it handles VAT/sales tax worldwide so a solo dev doesn't
  register in every jurisdiction.
- **Open-core**: business model where the core is open source and revenue
  comes from a paid tier around it (Meetily, Anarlog).
- **PPP (purchasing-power parity) pricing**: regional prices adjusted to
  local buying power (e.g. $69 US / ~$29–39 LATAM).
- **SKU**: one sellable variant of the product (e.g. the direct one-time
  license and a Mac App Store subscription are two SKUs of the same app).
- **P0/P1/P2 · S/M/L**: priority (before charging / next quarter / later) ·
  effort (< 1 day / days / weeks).

---

*Update log: created Jul 16, 2026 (strategy analysis session). Iteration 1
(Jul 17): repo re-sync — semantic/Spotlight/waveform cliffs removed by Band
4/D82–D83 work, receipts (D75) and support evidence (D76/D77) shipped, §1/§2/
§14/§15/§16 corrected accordingly. Iteration 2 (Jul 17): four live-web
research passes integrated (competitors; growth playbooks; monetization stack
+ WWDC 2026 — no Sherlock, window to WWDC 2027; wedge validation — regulated
professionals NOW, bilingual as in-wedge differentiator, Spain before LATAM);
added §0 execution spine; roadmap critique updated. Codex's band program
co-edits this file — keep folding its landings in rather than duplicating.
Iteration 3 (Jul 17, second refresh): licensing corrected to the stack
actually in use (Lemon Squeezy now, provider-agnostic seam, planned H2
migration — Stripe Managed Payments went GA Apr 29); Gancho sibling-project
examination (no sqlite-vec there either — both codebases converged on the
Accelerate linear-scan design; ported idea: stamp stored vectors with their
producing embedding model); market delta pass (Teams Phase 2 auto-block
begins Aug 2026; Otter MTD ruling imminent; Meetily code-stalled; Screenpipe
exited the meeting lane; viral SpeechAnalyzer benchmark lowers the raw-ASR
floor for every Mac indie); absorbed docs/design/claude-design-brief.md as
§17; added the §18 glossary; declared this the single go-forward planning
document.*
